## Описание

AREC (Audio Recording) - это специализированный скрипт для автоматической записи аудио в автомобиле с USB-микрофона BY-LM40 на Raspberry Pi Zero 2 W. Скрипт предназначен для работы в экстремальных условиях автомобиля: нестабильное питание, перепады температуры, вибрация, периодическое отсутствие интернета. Включает автоматическую загрузку файлов в облачное хранилище (Google Drive или Яндекс.Диск), синхронизацию времени через RTC модуль, восстановление после сбоев питания и оптимизацию для работы в автомобильной среде.

## Основные возможности

### Запись и обработка аудио
- 🎤 **Автоматическая запись** с USB-микрофона BY-LM40 через ALSA
- 🔄 **Перекрывающая запись** - без потери звука между файлами
- ⏱️ **Непрерывная запись** - разбивка на сегменты по 30 минут с перекрытием 5 секунд
- 🎵 **Поддержка форматов** - Opus, AAC, MP3 с настраиваемым качеством
- 🔧 **Оптимизация для Pi Zero 2 W** - минимальная нагрузка на систему
- 🛡️ **Улучшенная обработка ошибок** - детальное логирование и восстановление
- ⚡ **Стабильная запись** - использование arecord вместо PulseAudio для надежности

### Облачная интеграция
- ☁️ **Загрузка в облако** (Google Drive, Яндекс.Диск)
- 📤 **Очередь загрузки** - автоматическая обработка при появлении интернета
- 🔄 **Адаптивные повторные попытки** - до 15 попыток для быстрой сети, 5 для медленной
- 🗑️ **Автоочистка** - удаление файлов после успешной загрузки
- ⚡ **Параллельная загрузка** - до 3 потоков для быстрой сети
- 🎯 **Приоритизация файлов** - новые файлы загружаются первыми

### Управление временем
- 🕐 **Синхронизация времени** - автоматическая с RTC модулем DS3231
- 🌐 **NTP синхронизация** - при наличии интернета
- ⏰ **Планирование через systemd** - надежное управление расписанием

### Надежность и восстановление
- 🔄 **Восстановление после сбоев** - автоматическое при перезапуске
- 📁 **Управление хранилищем** - контроль размера (до 40 ГБ)
- 🛡️ **Маркеры восстановления** - защита от потери данных
- 📊 **Мониторинг системы** - логирование всех операций
- ⚡ **Graceful shutdown** - корректное завершение при получении сигналов
- 🔍 **Детальная диагностика** - коды ошибок и контекст для отладки
- 🌐 **Адаптивная сеть** - автоматическая оптимизация под скорость соединения
- 📈 **Мониторинг очереди** - предупреждения о переполнении хранилища

### Автомобильная специфика
- 🔌 **Работа с нестабильным питанием** - корректное завершение при отключении
- 🌡️ **Адаптация к температуре** - оптимизация для экстремальных условий
- 📶 **Умное управление сетью** - поддержка множественных Wi-Fi подключений
- 🚗 **Автомобильная оптимизация** - специальные настройки для работы в машине
- 🌐 **Адаптация к нестабильному интернету** - оптимизация для мобильных сетей
- ⚡ **Эффективная загрузка** - параллельная загрузка при хорошем соединении

## Системные требования

### Аппаратное обеспечение
- Raspberry Pi Zero 2 W
- USB-микрофон BY-LM40 (или совместимый)
- RTC модуль DS3231 (рекомендуется для автомобиля)
- SD-карта минимум 32 ГБ
- Стабильное интернет-соединение (для синхронизации времени)

### Программное обеспечение
- Raspberry Pi OS (Debian-based)
- FFmpeg
- rclone
- i2c-tools (для RTC)
- ntpdate (для синхронизации времени)
- Bash 4.0+

## Установка и настройка

### 1. Установка зависимостей

```bash
# Обновление системы
sudo apt update && sudo apt upgrade -y

# Установка необходимых пакетов
sudo apt install -y ffmpeg rclone i2c-tools ntpdate alsa-utils
```

### 2. Установка и настройка RTC модуля DS3231

Для корректной работы времени в автомобиле (без постоянного интернета) рекомендуется установить RTC модуль DS3231.

#### Подключение DS3231 к Raspberry Pi Zero 2 W:

| DS3231 | Raspberry Pi | Описание |
|--------|--------------|----------|
| VCC    | 3.3V (Pin 1) | Питание 3.3V |
| GND    | GND (Pin 6)  | Земля |
| SDA    | GPIO 2 (Pin 3) | Данные I2C |
| SCL    | GPIO 3 (Pin 5) | Тактовая линия I2C |

#### Настройка системы:

```bash
# Включение I2C интерфейса
sudo raspi-config
# Interface Options → I2C → Enable → Yes

# Добавление поддержки DS3231 в конфигурацию
echo 'dtoverlay=i2c-rtc,ds3231' | sudo tee -a /boot/config.txt

# Отключение fake-hwclock (заменяется реальным RTC)
sudo systemctl disable fake-hwclock
sudo systemctl stop fake-hwclock

# Перезагрузка для применения изменений
sudo reboot
```

#### Проверка установки:

```bash
# Проверка обнаружения RTC модуля
sudo i2cdetect -y 1

# Проверка работы RTC
sudo hwclock --show

# Синхронизация времени с RTC
sudo hwclock --hctosys

# Проверка текущего времени
date
```

#### Настройка автоматической синхронизации:

```bash
# Создание скрипта синхронизации времени
sudo nano /lib/udev/hwclock-set
```

Содержимое файла `/lib/udev/hwclock-set`:
```bash
#!/bin/sh
# Reset the System Clock to UTC if the hardware clock from which we
# are about to copy the time is in localtime.

dev=$1

if [ -e /run/systemd/system ] ; then
    exit 0
fi

if [ -e /dev/rtc ] ; then
    /sbin/hwclock --rtc=/dev/rtc --hctosys
fi

if [ -e /dev/rtc0 ] ; then
    /sbin/hwclock --rtc=/dev/rtc0 --hctosys
fi

exit 0
```

```bash
# Установка прав на файл
sudo chmod +x /lib/udev/hwclock-set

# Проверка работы RTC после перезагрузки
sudo reboot
sudo hwclock --show
```

#### Устранение неполадок RTC:

```bash
# Проверка подключения I2C
sudo i2cdetect -y 1
# Должен показать устройство на адресе 0x68

# Проверка логов
dmesg | grep rtc

# Ручная синхронизация
sudo hwclock --systohc  # Сохранить системное время в RTC
sudo hwclock --hctosys  # Загрузить время из RTC в систему

# Проверка точности RTC
sudo hwclock --show --verbose
```

### 3. Настройка Wi-Fi подключений

Для работы в автомобиле рекомендуется настроить несколько Wi-Fi сетей для автоматического переключения.

#### Просмотр текущих подключений:
```bash
# Список всех подключений
nmcli connection show

# Статус активных подключений
nmcli connection show --active

# Информация о доступных Wi-Fi сетях
nmcli device wifi list
```

#### Добавление новых Wi-Fi сетей:

**Способ 1: Через командную строку (рекомендуется)**
```bash
# Создание нового подключения
sudo nmcli connection add type wifi con-name "MyWiFi" ifname wlan0 ssid "MyWiFi"

# Настройка пароля
sudo nmcli connection modify "MyWiFi" wifi-sec.key-mgmt wpa-psk
sudo nmcli connection modify "MyWiFi" wifi-sec.psk "password123"

# Активация подключения
sudo nmcli connection up "MyWiFi"
```

**Способ 2: Создание файла вручную**
```bash
# Создание нового файла подключения
sudo nano /etc/NetworkManager/system-connections/MyWiFi.nmconnection
```

Содержимое файла:
```ini
[connection]
id=MyWiFi
uuid=12345678-1234-1234-1234-123456789abc
type=wifi

[wifi]
mode=infrastructure
ssid=MyWiFi
hidden=false

[ipv4]
method=auto

[ipv6]
addr-gen-mode=default
method=auto

[proxy]

[wifi-security]
key-mgmt=wpa-psk
psk=password123
```

```bash
# Установка прав доступа
sudo chmod 600 /etc/NetworkManager/system-connections/MyWiFi.nmconnection
sudo chown root:root /etc/NetworkManager/system-connections/MyWiFi.nmconnection

# Перезагрузка NetworkManager
sudo systemctl restart NetworkManager
```

#### Рекомендуемые сети для автомобиля:
- **Домашняя сеть** - для синхронизации при возвращении домой
- **Мобильная точка доступа** - для работы в дороге
- **Публичные Wi-Fi** - для использования в общественных местах

#### Управление подключениями:
```bash
# Удаление подключения
sudo nmcli connection delete "MyWiFi"

# Отключение от сети
sudo nmcli connection down "MyWiFi"

# Подключение к сети
sudo nmcli connection up "MyWiFi"

# Перезапуск Wi-Fi
sudo systemctl restart NetworkManager
```

#### Устранение проблем с Wi-Fi:
```bash
# Проверка статуса NetworkManager
sudo systemctl status NetworkManager

# Просмотр логов
sudo journalctl -u NetworkManager -f

# Сброс настроек Wi-Fi
sudo rm /etc/NetworkManager/system-connections/*
sudo systemctl restart NetworkManager
```

### 4. Настройка rclone

#### Для Google Drive:
```bash
rclone config
# Следуйте инструкциям для настройки Google Drive
# Имя удаленного: google.drive
```

#### Для Яндекс.Диска:
```bash
rclone config
# Следуйте инструкциям для настройки Яндекс.Диска
# Имя удаленного: yandex.disk
```

### 5. Настройка скрипта

Отредактируйте файл `arec.sh`:

```bash
nano arec.sh
```

#### Основные параметры:
- `SPLIT_TIME=1800` - длительность файла (30 минут)
- `OVERLAP_TIME=10` - время перекрытия между файлами (10 секунд)
- `AUDIO_FORMAT="opus"` - формат аудио (aac, opus, mp3)
- `BITRATE=64` - битрейт (кбит/с)
- `SAMPLE_FORMAT="S24_3LE"` - формат семплирования ALSA
- `SAMPLE_RATE=48000` - частота дискретизации (Гц)
- `MIC="default"` - источник звука (ALSA устройство)
- `CLOUD_SERVICE="yandex"` - облачный сервис
- `MAX_STORAGE_MB=40960` - максимальный размер хранилища (40 ГБ)

#### Параметры адаптивной сети:
- `MAX_PARALLEL_UPLOADS=3` - максимальное количество параллельных загрузок
- `NETWORK_SPEED_THRESHOLD=100` - порог медленной сети (мс ping)
- `SLOW_NETWORK_RETRY_DELAY=600` - задержка для медленной сети (секунды)
- `SLOW_NETWORK_MAX_RETRIES=5` - максимальные попытки для медленной сети
- `QUEUE_WARNING_THRESHOLD=80` - порог предупреждения о размере очереди (%)

### 6. Создание директорий

```bash
sudo mkdir -p /root/arec/pending
sudo chown root:root /root/arec
```

### 7. Запуск скрипта

```bash
# Сделать исполняемым
chmod +x arec.sh

# Запуск
sudo ./arec.sh
```

## Настройки скрипта

### Основные параметры записи

| Параметр | Описание | Значение по умолчанию |
|----------|----------|----------------------|
| `SPLIT_TIME` | Длительность одного файла (секунды) | 1800 (30 мин) |
| `OVERLAP_TIME` | Время перекрытия между файлами (секунды) | 10 |
| `OUTPUT_DIR` | Папка для сохранения файлов | `/root/arec` |
| `SAMPLE_RATE` | Частота дискретизации (Гц) | 48000 |
| `MIC` | Источник звука (пусто = автоопределение) | "" |

### Настройки кодирования

| Параметр | Описание | Возможные значения |
|----------|----------|-------------------|
| `AUDIO_FORMAT` | Формат аудио | aac, opus, mp3 |
| `BITRATE` | Битрейт (кбит/с) | 32, 64, 128, 256 |
| `SAMPLE_FORMAT` | Формат семплирования ALSA | S16_LE, S24_3LE, S32_LE |
| `SAMPLE_RATE` | Частота дискретизации (Гц) | 44100, 48000, 96000 |
| `MIC` | ALSA устройство | default, hw:0,0, hw:1,0 |

### Настройки облачного хранилища

| Параметр | Описание | Значение по умолчанию |
|----------|----------|----------------------|
| `CLOUD_SERVICE` | Облачный сервис | "yandex" |
| `DELETE_AFTER_UPLOAD` | Удалять после загрузки | true |
| `MAX_STORAGE_MB` | Максимальный размер хранилища (МБ) | 40960 (40 ГБ) |
| `MAX_RETRIES` | Максимальное количество попыток | 15 |
| `RETRY_DELAY` | Задержка между попытками (секунды) | 300 |

### Настройки адаптивной сети

| Параметр | Описание | Значение по умолчанию |
|----------|----------|----------------------|
| `MAX_PARALLEL_UPLOADS` | Максимальное количество параллельных загрузок | 3 |
| `NETWORK_SPEED_THRESHOLD` | Порог медленной сети (мс ping) | 100 |
| `SLOW_NETWORK_RETRY_DELAY` | Задержка для медленной сети (секунды) | 600 |
| `SLOW_NETWORK_MAX_RETRIES` | Максимальные попытки для медленной сети | 5 |
| `QUEUE_WARNING_THRESHOLD` | Порог предупреждения о размере очереди (%) | 80 |

## Принцип работы

### 1. Инициализация
- Синхронизация времени с RTC (если доступен)
- Настройка микрофона через ALSA
- Проверка доступности микрофона BY-LM40
- Проверка интернет-соединения
- Восстановление прерванных файлов
- Создание необходимых директорий с проверкой прав

### 2. Основной цикл с перекрытием
- **Запуск первого файла** - запись на 30 минут
- **Ожидание перекрытия** - через 29 минут 50 секунд
- **Запуск второго файла** - с перекрытием 10 секунд
- **Завершение первого файла** - обработка и загрузка
- **Завершение второго файла** - обработка и загрузка
- **Повтор цикла** - непрерывная работа

### 3. Перекрывающая запись
```
Время: 0:00 ────────────────────────────────── 30:00 ────────────────────────────────── 60:00
Файл1: [████████████████████████████████████████████████████████████████████████████████]
Файл2:                                    [████████████████████████████████████████████████████████████████████████████████]
```

**Преимущества:**
- ✅ **Нулевая потеря звука** между файлами
- ✅ **Надежность** - при сбое одного файла второй продолжает запись
- ✅ **Непрерывность** - звук записывается без прерываний
- ✅ **Стабильная работа** через ALSA (arecord) без PulseAudio
- ✅ **Автоматическое управление** процессами записи

### 4. Обработка ошибок
- **Детальное логирование** с кодами ошибок и контекстом
- **Graceful shutdown** при получении сигналов (INT, TERM, EXIT)
- **Автоматическое восстановление** микрофона при сбоях через ALSA
- **Проверка целостности** файлов после записи
- **Таймауты** для предотвращения зависаний
- **Унифицированная обработка** ошибок микрофона

### 5. Адаптивная обработка очереди
- **Проверка скорости сети** - автоматическое определение качества соединения
- **Параллельная загрузка** - до 3 потоков для быстрой сети
- **Последовательная загрузка** - 1 поток для медленной сети
- **Приоритизация файлов** - новые файлы загружаются первыми
- **Адаптивные повторные попытки** - разные параметры для разных скоростей сети

### 6. Управление хранилищем
- Контроль размера локального хранилища
- Автоматическая очистка старых файлов
- Приоритет новым записям
- Мониторинг очереди с предупреждениями
- Автоматическая очистка при критическом переполнении

## Форматы аудио

### Opus (рекомендуется)
- **Преимущества**: Высокое качество при низком битрейте, оптимизирован для голоса
- **Недостатки**: Менее совместим со старыми устройствами
- **Использование**: VoIP, стриминг, архивирование

### AAC
- **Преимущества**: Хорошее качество, широкоя совместимость
- **Недостатки**: Чуть больше размер файла
- **Использование**: Универсальное применение

### MP3
- **Преимущества**: Максимальная совместимость
- **Недостатки**: Больший размер файла, патентные ограничения
- **Использование**: Когда нужна максимальная совместимость

## Устранение неполадок

### Проблемы с микрофоном
```bash
# Проверка ALSA устройств
arecord -l

# Проверка доступности микрофона
arecord -D default -f S24_3LE -r 48000 -d 1 --quiet - >/dev/null 2>&1 && echo "OK" || echo "ERROR"

# Проверка подключения USB
lsusb | grep -i "ktmicro\|by-lm40"

# Восстановление ALSA
alsactl restore

# Проверка логов
grep -i "микрофон\|microphone\|arecord" /root/arec/arec.log | tail -10
```

### Проблемы с сетью
```bash
# Проверка интернет-соединения
ping -c 3 8.8.8.8

# Проверка rclone
rclone about yandex.disk:
```

### Проблемы с записью
```bash
# Проверка логов
tail -f /root/arec/arec.log

# Проверка места на диске
df -h /root/arec

# Проверка перекрывающей записи
grep "перекрывающей" /root/arec/arec.log

# Проверка ошибок arecord/ffmpeg
grep -E "(arecord|ffmpeg)" /root/arec/arec.log | grep "ОШИБКА"

# Проверка ALSA устройств
arecord -l
```

### Проблемы с RTC модулем
```bash
# Проверка подключения I2C
sudo i2cdetect -y 1
# Должен показать устройство на адресе 0x68

# Проверка работы RTC
sudo hwclock --show

# Проверка логов системы
dmesg | grep rtc

# Ручная синхронизация времени
sudo hwclock --hctosys

# Проверка точности RTC
sudo hwclock --show --verbose

# Проверка синхронизации в логах скрипта
grep "время" /root/arec/arec.log
```

### Предупреждения ALSA

При работе с USB-микрофонами на Raspberry Pi могут появляться предупреждения в логах:

```
Error opening PCM device hw:0: Device or resource busy
ALSA woke us up to read new data from the device, but there was actually nothing to read.
Most likely this is a bug in the ALSA driver 'snd_usb_audio'.
GetManagedObjects() failed: org.freedesktop.DBus.Error.NoReply
```

**Эти предупреждения не влияют на работу скрипта** и являются типичными для USB-аудиоустройств на Raspberry Pi.

#### Способы уменьшения логов ALSA:

**1. Настройка уровня логирования ALSA:**
```bash
# Редактирование конфигурации ALSA
sudo nano /etc/asound.conf
```

**2. Проверка состояния ALSA:**
```bash
# Проверка устройств
arecord -l

# Проверка доступности микрофона
arecord -D default -f S24_3LE -r 48000 -d 1 --quiet - >/dev/null 2>&1 && echo "OK" || echo "ERROR"
```

**3. Фильтрация логов systemd:**
```bash
# Просмотр логов без предупреждений ALSA
sudo journalctl -u arec.service -f | grep -v "alsa"

# Или только важные сообщения
sudo journalctl -u arec.service -f | grep -E "(ERROR|WARN|arec\.sh)"
```

**4. Восстановление ALSA при проблемах:**
```bash
# Восстановление настроек ALSA
alsactl restore

# Перезагрузка ALSA
sudo systemctl restart alsa-state
```

**5. Проверка работы после настройки:**
```bash
# Проверка статуса сервиса
sudo systemctl status arec.service

# Проверка логов скрипта
tail -f /root/arec/arec.log

# Проверка создания файлов
ls -la /root/arec/REC_*.opus
```

**Примечание:** Даже при настройке логирования предупреждения могут периодически появляться. Это нормально и не влияет на качество записи.

## Мониторинг

### Логи
- Основной лог: `/root/arec/arec.log`
- Логи FFmpeg: в основном логе
- Логи rclone: в основном логе
- Детальные коды ошибок с контекстом

### Статистика
- Количество записанных файлов
- Размер локального хранилища
- Количество файлов в очереди
- Статус загрузки
- Статус перекрывающей записи
- Коды ошибок и их частота

### Мониторинг перекрывающей записи
```bash
# Проверка активных процессов arecord и ffmpeg
ps aux | grep -E "(arecord|ffmpeg)"

# Проверка перекрытия в логах
grep "перекрывающей" /root/arec/arec.log | tail -10

# Проверка завершения файлов
grep "завершен" /root/arec/arec.log | tail -10

# Проверка ALSA устройств
arecord -l
```

### Мониторинг адаптивной сети
```bash
# Проверка скорости сети
ping -c 3 8.8.8.8 | grep "avg"

# Проверка параллельных загрузок
ps aux | grep rclone | wc -l

# Проверка размера очереди
du -sh /root/arec/pending

# Проверка предупреждений о переполнении
grep "Предупреждение" /root/arec/arec.log | tail -5
```

## Планирование и автозапуск

### Настройка расписания через systemd timers

Рекомендуется использовать systemd timers для надежного управления расписанием работы скрипта.

#### 1. Отключение автозапуска сервиса
```bash
# Отключить автозапуск сервиса
sudo systemctl disable arec.service
```

#### 2. Создание сервисов управления
```bash
# Создание сервиса запуска
sudo tee /etc/systemd/system/arec-start.service > /dev/null << 'EOF'
[Unit]
Description=Start Audio Recording Service
After=network.target

[Service]
Type=oneshot
ExecStart=/bin/systemctl start arec.service
RemainAfterExit=yes

[Install]
WantedBy=multi-user.target
EOF

# Создание сервиса остановки
sudo tee /etc/systemd/system/arec-stop.service > /dev/null << 'EOF'
[Unit]
Description=Stop Audio Recording Service

[Service]
Type=oneshot
ExecStart=/bin/systemctl stop arec.service
RemainAfterExit=yes

[Install]
WantedBy=multi-user.target
EOF
```

#### 3. Создание таймеров
```bash
# Создание таймера запуска (06:00)
sudo tee /etc/systemd/system/arec-start.timer > /dev/null << 'EOF'
[Unit]
Description=Start Audio Recording at 06:00
Requires=arec-start.service

[Timer]
OnCalendar=*-*-* 06:00:00
Persistent=true

[Install]
WantedBy=timers.target
EOF

# Создание таймера остановки (23:30)
sudo tee /etc/systemd/system/arec-stop.timer > /dev/null << 'EOF'
[Unit]
Description=Stop Audio Recording at 23:30
Requires=arec-stop.service

[Timer]
OnCalendar=*-*-* 23:30:00
Persistent=true

[Install]
WantedBy=timers.target
EOF
```

#### 4. Активация таймеров
```bash
# Перезагрузка systemd
sudo systemctl daemon-reload

# Включение таймеров
sudo systemctl enable arec-start.timer
sudo systemctl enable arec-stop.timer

# Запуск таймеров
sudo systemctl start arec-start.timer
sudo systemctl start arec-stop.timer
```

#### 5. Проверка статуса таймеров
```bash
# Просмотр активных таймеров
sudo systemctl list-timers | grep arec

# Просмотр статуса конкретного таймера
sudo systemctl status arec-start.timer
sudo systemctl status arec-stop.timer
```

#### 6. Управление расписанием
```bash
# Просмотр следующего запуска
sudo systemctl list-timers | grep arec

# Ручной запуск/остановка
sudo systemctl start arec.service
sudo systemctl stop arec.service

# Изменение времени (редактировать файлы таймеров)
sudo nano /etc/systemd/system/arec-start.timer
sudo nano /etc/systemd/system/arec-stop.timer
sudo systemctl daemon-reload
```

#### 7. Просмотр логов
```bash
# Логи таймеров
sudo journalctl -u arec-start.timer -f
sudo journalctl -u arec-stop.timer -f

# Логи сервиса
sudo journalctl -u arec.service -f
```

#### 8. Примеры расписания

**Изменение времени запуска на 08:00:**
```bash
# Редактировать файл таймера
sudo nano /etc/systemd/system/arec-start.timer

# Изменить строку:
OnCalendar=*-*-* 08:00:00

# Перезагрузить systemd
sudo systemctl daemon-reload
```

**Запуск только в рабочие дни:**
```bash
# Изменить строку в arec-start.timer:
OnCalendar=Mon..Fri 06:00:00

# Изменить строку в arec-stop.timer:
OnCalendar=Mon..Fri 23:30:00
```

**Запуск с интервалом (каждые 2 часа):**
```bash
# Изменить строку в arec-start.timer:
OnCalendar=*-*-* 06/2:00:00
```

#### 9. Преимущества systemd timers над cron

| Критерий | Cron | Systemd Timers |
|----------|------|----------------|
| **Управление процессами** | ❌ Простое убийство | ✅ Корректное завершение |
| **Логирование** | ❌ Базовое | ✅ Детальное |
| **Мониторинг** | ❌ Нет | ✅ Полный контроль |
| **Надежность** | ❌ Может зависнуть | ✅ Автоматическое восстановление |
| **Зависимости** | ❌ Нет | ✅ Управление зависимостями |



### systemd сервис (базовый)

Для постоянной работы скрипта используется systemd сервис:

```bash
# Создание сервиса
sudo nano /etc/systemd/system/arec.service
```

```ini
[Unit]
Description=AREC Audio Recording Service
After=network.target

[Service]
Type=simple
User=root
WorkingDirectory=/root/arec
ExecStart=/root/arec/arec.sh
Restart=always
RestartSec=10

[Install]
WantedBy=multi-user.target
```

```bash
# Активация сервиса
sudo systemctl enable arec.service
sudo systemctl start arec.service

# Управление сервисом
sudo systemctl status arec.service
sudo systemctl stop arec.service
sudo systemctl restart arec.service
```

**Примечание:** Для управления расписанием используйте systemd timers (см. выше).

## Безопасность

### Рекомендации
- Используйте отдельного пользователя для записи
- Ограничьте доступ к логам
- Регулярно обновляйте систему
- Используйте VPN при необходимости

### Права доступа
```bash
# Установка прав
chmod 600 /root/arec/arec.log
chmod 700 /root/arec
```

## Производительность

### Оптимизация для Pi Zero 2 W
- Использование Opus для экономии CPU
- Фоновая обработка очереди
- Оптимизированные параметры FFmpeg
- Перекрывающая запись с минимальным потреблением ресурсов
- Эффективное управление двумя процессами arecord+ffmpeg
- Адаптивная загрузка в зависимости от скорости сети
- Оптимизированные операции с файлами

### Мониторинг ресурсов
```bash
# Использование CPU
top -p $(pgrep ffmpeg)

# Использование памяти
free -h

# Использование диска
df -h

# Мониторинг перекрывающей записи
ps aux | grep ffmpeg | wc -l  # Должно быть 2 процесса

# Проверка нагрузки на систему
iostat 1 5

# Мониторинг сетевых процессов
ps aux | grep rclone | wc -l  # Количество активных загрузок

# Проверка скорости сети
ping -c 3 8.8.8.8 | grep "avg"
```

## Рекомендации для работы в автомобиле

При установке устройства в автомобиле возникают специфические проблемы, связанные с экстремальными условиями эксплуатации. Ниже приведены рекомендации для обеспечения стабильной работы.

### Проблемы и решения

#### 1. Питание

**Проблемы:**
- Скачки напряжения при запуске/остановке двигателя
- Нестабильное питание при низком заряде АКБ
- Короткие отключения при переключении режимов

**Решения:**
- Использовать стабилизатор напряжения 12V→5V
- Добавить конденсатор для сглаживания скачков
- Мониторинг напряжения в скрипте

```bash
# Добавить в скрипт проверку напряжения
check_power() {
    local voltage=$(vcgencmd measure_volts | cut -d= -f2 | cut -dV -f1)
    if (( $(echo "$voltage < 4.5" | bc -l) )); then
        log_message "Низкое напряжение: ${voltage}V"
        return 1
    fi
    return 0
}
```

#### 2. Температура

**Проблемы:**
- Перегрев летом (до +60°C)
- Переохлаждение зимой (до -30°C)
- Быстрые перепады при прогреве/остывании

**Решения:**
- Установить пассивное охлаждение (радиатор)
- Использовать термозащитный корпус
- Мониторинг температуры

```bash
# Добавить в скрипт мониторинг температуры
check_temperature() {
    local temp=$(vcgencmd measure_temp | cut -d= -f2 | cut -d\' -f1)
    if (( $(echo "$temp > 70" | bc -l) )); then
        log_message "Высокая температура: ${temp}°C"
        return 1
    fi
    return 0
}
```

#### 3. Вибрация

**Проблемы:**
- Постоянная вибрация двигателя
- Тряска на неровностях дороги
- Ослабление контактов

**Решения:**
- Использовать антивибрационные крепления
- Установить в защищенный корпус
- Надежно закрепить SD-карту

#### 4. Сеть

**Проблемы:**
- Нестабильный Wi-Fi сигнал
- Частые переключения между сетями
- Медленная загрузка в движении
- Переменная скорость мобильного интернета

**Решения:**
- Использовать мобильный интернет как резерв
- Установить улучшенную антенну Wi-Fi
- Настроить кэширование для офлайн-работы
- **Адаптивная загрузка** - автоматическая оптимизация под скорость сети
- **Параллельная загрузка** - эффективное использование быстрого соединения
- **Приоритизация файлов** - новые записи загружаются первыми

```bash
# Адаптивная проверка сети (уже реализована в скрипте)
check_network_speed() {
    local ping_time=$(ping -c 3 8.8.8.8 | grep "avg" | cut -d'/' -f5 | cut -d'.' -f1)
    if [ "$ping_time" -gt 100 ]; then
        echo "slow"  # Медленная сеть
        return 1
    else
        echo "fast"  # Быстрая сеть
        return 0
    fi
}
```

#### 5. Хранилище

**Проблемы:**
- Повреждение SD-карты от вибрации
- Коррупция файлов при внезапном отключении
- Переполнение при длительном отсутствии интернета
- Накопление файлов в очереди при нестабильной сети

**Решения:**
- Использовать высококачественную SD-карту (Class 10, U3)
- Регулярно проверять файловую систему
- Настроить автоматическую очистку старых файлов
- **Мониторинг очереди** - предупреждения о переполнении
- **Приоритетная очистка** - удаление старых файлов при критическом переполнении
- **Адаптивная загрузка** - эффективное использование доступной сети

```bash
# Мониторинг размера очереди (уже реализовано в скрипте)
check_queue_size() {
    local pending_size=$(get_dir_size_mb "$PENDING_DIR")
    local usage_percent=$((pending_size * 100 / MAX_STORAGE_MB))
    
    if [ "$usage_percent" -gt 80 ]; then
        log_error "Критически мало места: ${usage_percent}%"
        return 1
    fi
    return 0
}
```

#### 6. Аудио

**Проблемы:**
- Шум двигателя и дороги
- Электромагнитные помехи
- Проблемы с USB-микрофоном при вибрации

**Решения:**
- Использовать микрофон с шумоподавлением
- Настроить фильтрацию фонового шума в FFmpeg
- Надежно закрепить USB-микрофон

### Системный мониторинг

```bash
# Добавить в скрипт системный мониторинг
system_health_check() {
    local cpu_temp=$(vcgencmd measure_temp | cut -d= -f2 | cut -d\' -f1)
    local voltage=$(vcgencmd measure_volts | cut -d= -f2 | cut -dV -f1)
    local memory=$(free | grep Mem | awk '{printf "%.1f", $3/$2 * 100.0}')
    
    log_message "Система: CPU=${cpu_temp}°C, V=${voltage}V, RAM=${memory}%"
    
    if (( $(echo "$cpu_temp > 70" | bc -l) )); then
        log_message "ПРЕДУПРЕЖДЕНИЕ: Высокая температура CPU"
    fi
    
    if (( $(echo "$voltage < 4.5" | bc -l) )); then
        log_message "ПРЕДУПРЕЖДЕНИЕ: Низкое напряжение"
    fi
}
```

### Автоматическое восстановление

```bash
# Добавить в скрипт автоматическое восстановление
auto_recovery() {
    # Проверка файловой системы
    if ! fsck -n /dev/mmcblk0p2 >/dev/null 2>&1; then
        log_message "Ошибка файловой системы, требуется перезагрузка"
        sudo reboot
    fi
    
    # Проверка места на диске
    local disk_usage=$(df / | tail -1 | awk '{print $5}' | sed 's/%//')
    if [ "$disk_usage" -gt 90 ]; then
        log_message "Критически мало места на диске: ${disk_usage}%"
        cleanup_old_files
    fi
}
```

### Рекомендации по оборудованию

#### Обязательные компоненты:
- **Корпус**: Защищенный, с вентиляцией
- **Питание**: Стабилизатор 12V→5V, конденсатор
- **SD-карта**: Высокого класса (Class 10, U3)
- **Микрофон**: С шумоподавлением
- **Крепление**: Антивибрационное

#### Дополнительные компоненты:
- **RTC модуль**: DS3231 для точного времени
- **Охлаждение**: Радиатор для процессора
- **Антенна**: Улучшенная Wi-Fi антенна
- **Резервное питание**: Внешний аккумулятор

### Безопасность

- Установить устройство в скрытом месте
- Использовать защищенный корпус
- Настроить шифрование данных
- Регулярно обновлять ПО

### Правовые аспекты

- Убедиться в законности записи в вашей юрисдикции
- Получить согласие на запись от всех участников
- Соблюдать требования по хранению персональных данных
- Не использовать запись в коммерческих целях без разрешения

## Поддержка

При возникновении проблем:
1. Проверьте логи: `tail -f /root/arec/arec.log`
2. Убедитесь в корректности настроек rclone
3. Проверьте подключение микрофона через ALSA: `arecord -l`
4. Убедитесь в стабильности интернет-соединения
5. Проверьте системные параметры (температура, напряжение)
6. Проверьте работу перекрывающей записи: `grep "перекрывающей" /root/arec/arec.log`
7. Убедитесь в наличии процессов arecord/ffmpeg: `ps aux | grep -E "(arecord|ffmpeg)"`
8. Проверьте коды ошибок в логах: `grep "ОШИБКА" /root/arec/arec.log | tail -10`
9. Проверьте скорость сети: `ping -c 3 8.8.8.8 | grep "avg"`
10. Проверьте размер очереди: `du -sh /root/arec/pending`
11. Проверьте активные загрузки: `ps aux | grep rclone | wc -l`
12. Проверьте предупреждения о переполнении: `grep "Предупреждение" /root/arec/arec.log | tail -5`
13. При проблемах с микрофоном используйте: `alsactl restore`

## Изменения в версии

### v2.1 - Стабильная запись через ALSA
- ✅ **Переход на arecord** - стабильная запись без PulseAudio
- ✅ **Упрощенная архитектура** - меньше зависимостей
- ✅ **Улучшенная совместимость** - POSIX совместимые команды
- ✅ **Оптимизированный код** - удалено дублирование
- ✅ **Унифицированная обработка** - единые функции для похожих задач
- ✅ **Systemd timers** - надежное управление расписанием вместо cron

### v2.0 - Перекрывающая запись
- ✅ **Перекрывающая запись** - без потери звука между файлами
- ✅ **Улучшенная обработка ошибок** - детальное логирование
- ✅ **Graceful shutdown** - корректное завершение работы
- ✅ **Адаптивная сеть** - оптимизация под скорость соединения
- ✅ **Параллельная загрузка** - до 3 потоков для быстрой сети
- ✅ **Мониторинг очереди** - предупреждения о переполнении
- ✅ **Приоритизация файлов** - новые файлы загружаются первыми
- ✅ **Оптимизация для Pi Zero 2 W** - минимальная нагрузка на систему

## Лицензия

Скрипт распространяется свободно для личного использования.
